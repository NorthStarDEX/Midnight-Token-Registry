{
  "name": "@northstardex/midnight-token-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only indexer for the Midnight Asset Registry contract.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "index": "node scripts/index.mjs",
    "validate:requests": "node scripts/validate-requests.mjs",
    "check:pr": "node scripts/check-pr.mjs"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "0.16.0"
  }
}
