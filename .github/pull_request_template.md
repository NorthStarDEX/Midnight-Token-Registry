## Request

Token color:

Action:

## Checklist

- [ ] I added exactly one JSON file under `requests/preprod/`.
- [ ] The filename matches the token color.
- [ ] `npm run validate:requests` passes.
- [ ] Image URL, if provided, is `ipfs://`, `/ipfs/`, or `https://`.
